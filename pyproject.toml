[project]
name = "agentic-rag-platform"
version = "0.1.0"
description = "Production-oriented retrieval-augmented agent platform"
requires-python = ">=3.10"
dependencies = [
    "sentence-transformers>=3.0",
    "chromadb>=0.5",
    "rank-bm25>=0.2.2",
    "pypdf>=4.0",
    "pydantic>=2.0",
    "typer>=0.12",
]

[tool.pytest.ini_options]
pythonpath = ["."]
